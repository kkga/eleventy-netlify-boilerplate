---
layout: layouts/base.njk
section: post
---

<article>
  <header>
    <h1>{{ title }}</h1>
    <small>
      {% if summary %}
      {{ summary }}
      <br />
      {% endif %}
      <time datetime="{{ page.date | machineDate }}">{{ page.date | readableDate }}</time>

      {% if tags %}
      <span>&middot;</span>
      {% for tag in tags %} {%- if tag != "post" -%} {% set tagUrl %}/tags/{{
        tag
      }}/{% endset %}
      <a href="{{ tagUrl | url }}" rel="tag">#{{ tag }}</a>
      {%- endif -%} {% endfor %} {% endif %}
    </small>
  </header>

  {{ layoutContent | safe }}
</article>
<hr />
<nav>
  <a href="{{ '/writing/' | url }}">← Writing index</a>
</nav>
