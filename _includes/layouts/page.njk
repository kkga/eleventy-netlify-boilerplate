---
layout: layouts/base.njk
section: page
---

<header>
  <h1>{{ title }}</h1>

  {% if subtext %}
  <small>
    {{ subtext }}. Updated
    <time datetime="{{ page.date | machineDate }}">{{
      page.date | relativeDate
    }}</time
    >.
  </small>
  {% endif %}
</header>

{{ layoutContent | safe }}
