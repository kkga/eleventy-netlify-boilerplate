---
layout: layouts/base.njk
section: page
---

<header>
	<h1>{{ title }}</h1>

	{% if subtext %} 
	<small>
	  {{subtext}}{% if date %}. Updated <time datetime="{{ date | machineDate }}">{{ date | readableDate }}</time>.{% endif %}
	</small>
	{% endif %}
</header>

{{ layoutContent | safe }}
